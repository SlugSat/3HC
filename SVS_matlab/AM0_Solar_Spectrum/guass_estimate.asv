function [spectrum] = guass_estimate(lamda,bandwidth)

    wavelength = (lamda-bandwidth*5):1:(lamda+bandwidth*5);
    r_intensity = exp(-( ( 4.*log(2).*((wavelength-lamda).^2)) ./ (2.*sqrt(2.*log(2)).*bandwidth) ));
    spectrum = [wavelength.' r_intensity.'];
end
